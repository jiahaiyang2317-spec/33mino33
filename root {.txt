:root {
    --ios-blue: #007AFF;
    --glass-opacity: rgba(255, 255, 255, 0.12); /* 提高透明度 */
    --glass-border: rgba(255, 255, 255, 0.4);
}
body {
    margin: 0;
    padding: 0;
    min-height: 100vh;
    /* 更鲜艳的 iOS 26 极光背景 */
    background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 25%, #a1c4fd 70%, #c2e9fb 100%);
    background-size: 400% 400%;
    animation: auroraFlow 15s ease infinite alternate;
    font-family: -apple-system, "SF Pro Display", "PingFang SC", sans-serif;
    display: flex;
    justify-content: center; /* 水平居中 */
    align-items: flex-start; /* 顶部对齐，由容器控制间距 */
    overflow-x: hidden;
}
@keyframes auroraFlow {
    0% { background-position: 0% 50%; }
    100% { background-position: 100% 50%; }
}
/* 四角图标样式 */
.corner-img {
    position: fixed;
    width: 62px;
    height: 62px;
    z-index: 1000;
    border-radius: 15px;
    object-fit: cover;
    box-shadow: 0 10px 20px rgba(0,0,0,0.1);
    border: 1px solid var(--glass-border);
}
.top-left { top: 25px; left: 25px; }
.top-right { top: 25px; right: 25px; }
.bottom-left { bottom: 25px; left: 25px; }
.bottom-right { bottom: 25px; right: 25px; }
/* 主容器居中控制 */
.app-container {
    width: 100%;
    max-width: 400px;
    padding: 90px 20px 40px 20px;
    display: flex;
    flex-direction: column;
    align-items: center; /* 确保卡片在容器内居中 */
    box-sizing: border-box;
}
/* 极致透明磨砂卡片 */
.ios-card {
    width: 100%;
    background: var(--glass-opacity);
    /* 核心：极致模糊与色彩提升 */
    backdrop-filter: blur(65px) saturate(230%) brightness(105%);
    -webkit-backdrop-filter: blur(65px) saturate(230%) brightness(105%);
    border-radius: 40px;
    padding: 30px;
    margin-bottom: 25px;
    border: 0.8px solid var(--glass-border);
    box-shadow: 0 15px 35px rgba(0,0,0,0.06);
    display: flex;
    flex-direction: column;
    align-items: center; /* 内部元素居中 */
    text-align: center;
    box-sizing: border-box;
}
.tag { font-size: 0.75rem; font-weight: 800; color: rgba(0,0,0,0.3); letter-spacing: 2px; margin-bottom: 8px; }
.timer {
    font-size: 5.2rem;
    font-weight: 800;
    color: #1d1d1f;
    letter-spacing: -4px;
    margin: 10px 0;
    font-variant-numeric: tabular-nums;
}
.main-text { font-size: 1.4rem; font-weight: 700; color: #1d1d1f; }
.main-text b { color: var(--ios-blue); font-size: 1.9rem; }
/* 按钮与交互 */
.btn-row { display: flex; gap: 12px; margin-top: 15px; width: 100%; justify-content: center; }
button {
    border: none;
    padding: 15px 32px;
    border-radius: 22px;
    font-weight: 700;
    font-size: 1rem;
    cursor: pointer;
    transition: 0.2s;
}
.primary { background: var(--ios-blue); color: white; box-shadow: 0 8px 20px rgba(0,122,255,0.25); }
.secondary { background: rgba(0,0,0,0.06); color: #1d1d1f; }
button:active { transform: scale(0.94); }
.input-group { width: 100%; }
input {
    width: 100%;
    padding: 14px;
    margin: 8px 0;
    border: none;
    border-radius: 16px;
    background: rgba(0,0,0,0.06);
    font-size: 1rem;
    outline: none;
    box-sizing: border-box;
}
.full-width { width: 100%; }
